Check style violations in the changed files. Report any lines that exceed the
project line-length limit. Flag unused imports. Note any inconsistent naming
conventions against the project style guide.
