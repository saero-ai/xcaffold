kind: config
version: "1.1"
project:
  name: settings-example
  settings:
    mcpServers:
        filesystem:
            command: "npx"
            args: ["-y", "@modelcontextprotocol/server-filesystem", "/workspace"]
