provider: claude
version: "1.0"
kinds:
  agent:
    name: { support: xcaffold-only }
    description: { support: required }
    model: { support: optional }
    effort: { support: optional }
    max-turns: { support: optional }
    tools: { support: optional }
    disallowed-tools: { support: optional }
    readonly: { support: optional }
    permission-mode: { support: optional }
    disable-model-invocation: { support: optional }
    user-invocable: { support: optional }
    background: { support: optional }
    isolation: { support: optional }
    memory: { support: optional }
    color: { support: xcaffold-only }
    initial-prompt: { support: optional }
    skills: { support: optional }
    rules: { support: xcaffold-only }
    mcp: { support: xcaffold-only }
    assertions: { support: xcaffold-only }
    mcp-servers: { support: optional }
    hooks: { support: optional }
    targets: { support: xcaffold-only }
  skill:
    name: { support: xcaffold-only }
    description: { support: optional }
    when-to-use: { support: optional }
    license: { support: optional }
    allowed-tools: { support: optional }
    disable-model-invocation: { support: optional }
    user-invocable: { support: optional }
    argument-hint: { support: optional }
    artifacts: { support: optional }
    references: { support: xcaffold-only }
    scripts: { support: xcaffold-only }
    assets: { support: xcaffold-only }
    examples: { support: xcaffold-only }
    targets: { support: xcaffold-only }
  rule:
    always-apply: { support: unsupported }
    description: { support: optional }
    activation: { support: unsupported }
    name: { support: xcaffold-only }
    paths: { support: optional }
    exclude-agents: { support: unsupported }
    targets: { support: xcaffold-only }
