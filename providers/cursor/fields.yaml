provider: cursor
version: "1.0"
kinds:
  agent:
    name: { support: unsupported }
    description: { support: optional }
    model: { support: optional }
    effort: { support: optional }
    max-turns: { support: unsupported }
    tools: { support: unsupported }
    disallowed-tools: { support: unsupported }
    readonly: { support: optional }
    permission-mode: { support: unsupported }
    disable-model-invocation: { support: unsupported }
    user-invocable: { support: unsupported }
    background: { support: optional }
    isolation: { support: unsupported }
    memory: { support: unsupported }
    color: { support: unsupported }
    initial-prompt: { support: unsupported }
    skills: { support: unsupported }
    rules: { support: unsupported }
    mcp: { support: unsupported }
    assertions: { support: unsupported }
    mcp-servers: { support: unsupported }
    hooks: { support: unsupported }
    targets: { support: unsupported }
  skill:
    name: { support: unsupported }
    description: { support: optional }
    when-to-use: { support: unsupported }
    license: { support: unsupported }
    allowed-tools: { support: unsupported }
    disable-model-invocation: { support: unsupported }
    user-invocable: { support: unsupported }
    argument-hint: { support: unsupported }
    artifacts: { support: optional }
    targets: { support: unsupported }
  rule:
    always-apply: { support: optional }
    description: { support: optional }
    activation: { support: optional }
    name: { support: unsupported }
    paths: { support: optional }
    exclude-agents: { support: unsupported }
    targets: { support: unsupported }
