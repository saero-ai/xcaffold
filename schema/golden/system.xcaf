# Preview — parser support pending. This file is NOT validated by the CI
# test (golden_test.go skips it).
kind: system
version: "1.0"
name: golden-system-preview
description: "Preview — parser support pending."
